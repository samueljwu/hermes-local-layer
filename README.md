# Hermes Local Layer (filtered public mirror)

This repository is an automatically generated, privacy-filtered showcase of reusable local Hermes operating-layer code and documentation.

```text
┌──────────────────────────────┐
│   PRIVATE LOCAL HERMES TREE  │
│ systems · harnesses · skills │
└──────────────┬───────────────┘
               │ allowlist + staged-tree leak scan
               ▼
┌──────────────────────────────┐
│ FILTERED PUBLIC LOCAL LAYER  │
├──────────────────────────────┤
│ reusable scripts and tests   │
│ safe skills and docs         │
│ harmless structure stubs     │
└──────────────┬───────────────┘
               │
               ▼
   personal data · credentials
   runtime state · private rules
          OMITTED ENTIRELY
```

It is not the private knowledge backup and is not a complete `/home/hermes` installation. Personal wiki, journal, task, feed, runtime, credential, and private scanner-rule content is omitted; a few structural pages are harmless placeholders.

Do not commit secrets here. The publication job copies only allowlisted text files, scans the complete staged tree, and rejects blocked paths and credential-like content before push.
